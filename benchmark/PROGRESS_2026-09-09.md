# Conversion fidelity progress — September 9, 2026

The ten-app functional/usability goal is still open. Eleven pinned public
exports can be restored: the five `samples/real` apps and six Microsoft business
exports. All generate valid code. The five regression apps and Microsoft
Employee Ideas pass the short startup check. The other five Microsoft apps
fail startup, with saved views now exposing previously silent missing identity
mappings and unsupported relative-date filters. All three default first-action
probes fail. Populated Employee Ideas passes 27 checks through mobile validation,
custom text questions, submission, persistence, reload and reopening. Its
optional voting probe fails: the UI displays one vote while the server retains
zero after unsupported Relate. Ratings, attachments, manager workflows and
complete app usability remain unassessed.
No fixture or clean loading screen
is counted as a usable real app.

## Reusable fixes and evidence

| Change | Executed evidence |
|---|---|
| Editable gallery identity and scope | Two contacts boot with distinct defaults. Editing row two changes only its persisted record; typing, caret selection and the actual input node survive updates and sorting. Save/reload, reset, selector records, disabled inputs and decoded row images pass in Chromium. |
| Parent selection | A child awaits its save, queues `Select(Parent)`, finishes its remaining state updates, and invokes the source gallery handler once with the correct record. |
| Timer lifecycle | A source-shaped loading timer initializes a collection and navigates to a ready screen. Focus, elapsed Value, repeat termination, screen pause/resume, reset and restart pass with a controlled Chromium clock. |
| Common Power Fx syntax | Block/line comments, logical words and nested action chains have executed value/branch-order checks and a generated loading-flow fixture. |
| Sorting | Normalized fields, bare/qualified directions and multiple column/order pairs have executed tests; gallery reordering retains edits and focus. |
| Record aliases and disambiguation | Nested `As`, table-qualified record fields and global bypass have executed semantic tests. Gallery aliases persist through nested LookUp and awaited correct-row saves in Chromium. Global source references retain collection/Sheet routing and schema inference. |
| Projection, membership and grouping | Tests cover case-sensitive/insensitive membership, single-column tables, typed multi-column grouping and nested ForAll/Ungroup. A generated browser journey removes only the matching row and immediately updates grouped totals. |
| Conditional branches | Multi-condition If keeps every branch and optional fallback; Switch evaluates its subject once. Executed tests cover selected async actions and skipped conditions; a generated grouped-total calculation uses the later If branch. The original Microsoft exports contain over 1,200 multi-condition If calls. |
| Local inspection drafts | SaveData/LoadData/ClearData retain nested values and dates across browser reloads. Chromium verifies append behavior, app/user isolation, scoped clearing, corrupt-cache recovery and preserving the prior saved draft after a quota failure. Inspection's source cache initialization now passes the startup check. |
| Dynamic input defaults and disabled states | The browser found that a saved record loaded while its input stayed empty. Standalone input defaults are now wired; restored note/date/checkbox values, Reset and edit/caret retention through unrelated updates pass. Source DisplayMode disables Save for an empty note and enables it while typing. Existing form record handling remains intact. |
| Dataverse contracts and source keys | The decoder reads all six real exports. A native-format browser fixture edits the second row through logical field names, retains its source primary key, selects exported choice labels, creates/deletes and reloads records. Zero, false, lookup snapshots and multiple choices persist. Invalid choices, conflicting aliases and changed/missing primary keys fail without partial writes or duplicate records. |
| Real generated server gate | The real-app soak now executes generated setup, table reads and choice metadata reads. A deliberately missing setup helper fails this gate despite valid syntax. Separate execution across the six Microsoft exports covers 124 tables and 302 choice fields. |
| Apps Script date transport | Nested Date values become ISO text before calling google.script.run. Unit tests retain the client Date and reject invalid/circular values. The browser bridge rejects raw Date objects; the native fixture saves a source Date formula through the supported transport. |
| Complete source time/validation formulas | TimeValue and Text retain fractional seconds, hour/minute semantics and output-language selection. IsMatch/MatchAll, Find and deferred IsBlankOrError reproduce the complete Inspection Manager validation formula. The unchanged Milestones timestamp formula is executed for current, older, blank and localized values. The generated UI reacts while typing and switching record/language inputs. |
| Canvas and typed controls | Source layout metadata and App/screen formulas survive parsing and enter the ledger. Chromium verifies manual/automatic containers, local DataCard child positions, numeric breakpoints, hidden-screen properties, minimum-width scrolling, uniform scaling and preserved edits/focus/caret on resize. Boolean toggle Value corrects real desktop routing and themes; slider Value and button Text retain their types. |
| Screen exit initialization | OnHidden runs on exit with the source screen's Self reference, before destination OnVisible. Awaited callbacks cannot invoke superseded entry handlers. Generated tests verify state transfer and surfaced exit errors. The original Employee Ideas loading-screen OnHidden initializes its mobile width and padding; the formerly collapsed landing page becomes readable and Browse campaigns works. |
| Navigation records and screen locals | Navigate retains literal context records, with original variable names, before target bindings/OnVisible. UpdateContext retains its defining screen across navigation and awaited saves. Generated Node and Chromium journeys select the second contact, edit only that persisted row, preserve hidden-screen bindings and local state, clear to Blank without exposing a global, and reload saved data. Runtime tests cover case-insensitive symbols, zero/false, global bypass, lazy record/local/global precedence and empty Back history. Milestones' previously dropped first-run dialog is restored. |
| Real first-action evidence | Solution-aware probes now fail on missing migrated users, unsupported relative-date filters and connector calls. Source, converter and solution hashes must match before evidence can be merged. Earlier empty-data first-action passes did not establish source query semantics. |
| Saved views | Companion solution FetchXML supplies filters missing from canvas exports. Native generated Node/Chromium tests cover nested AND/OR, typed comparisons, nulls, date instants, explicit multi-column ordering, Search, save/reload and rejected unsupported queries. Missing metadata fails on empty tables and cannot use LLM fallback. Localized choice ordering is rejected unless raw numeric order is explicitly requested; text collation and implicit primary-key ordering remain ledgered approximations. |
| Startup identity | Source OnStart now waits for the actual session identity instead of capturing a permanently blank User().Email. Current-user view filters match exactly one migrated systemuser by email; absent, anonymous or duplicate matches fail. This does not implement Dataverse security. |
| Legacy template selection | A structural GalleryTemplate's OnSelect is retained when the gallery does not carry its own action. The native saved-view fixture selects the second row through Select(Parent). In the real Employee Ideas export, campaign selection now opens the correct campaign detail and its new-idea screen. |
| Native card layouts | Form/FluidGrid cards use source X/Y order and rows, minimum-width wrapping, WidthFit expansion and maximum row heights. Hidden cards leave the flow. A generated Chromium fixture verifies resize, scrolling, footer access and retained input nodes/text/focus/caret. Gallery template dimensions exist before first-row bindings. Source text overflow clips or scrolls as exported. |
| Typed collection aliases | Explicit table lineage retains exported logical/display aliases through Collect, Patch, Remove, local cache restoration and UpdateIf. Conflicts fail before mutation. Executed tests cover empty/transitive lineage, unrelated collections, first-match changes and inner-row versus outer-gallery scope. Chromium edits only the second draft, saves it through generated Code.gs, reloads and restores aliases from the local cache. |
| Source error recovery | IsError defers synchronous/async failures and distinguishes successful Blank/zero/false from errors. Direct nonfinite numeric results are errors; full Power Fx error-value propagation remains unimplemented. The unchanged real app catches the unsupported Teams post, shows its own warning and completes navigation to success. |
| Lookup snapshot aliases | Exported target-table metadata normalizes logical/display names recursively. Tests retain typed values, key-only identities and extra formula columns; ambiguous polymorphic targets, conflicting aliases and invalid nested values fail before any row write. A Chromium draft fixture reads target names through source display aliases. These are snapshots, not live relationship traversal. |
| Input modes and gallery ownership | Literal/conditional TextMode selects single-line, multiline or password controls. Chromium verifies standalone and per-row edits, transferred OnChange handlers, focus/caret and newline persistence. A pinpoint runtime regression and browser append case prevent a nested gallery template from replacing its owning gallery's template. Full nested-gallery rendering remains unverified. |
| Populated real workflow | One authored user, four campaigns and three questions are written through generated Code.gs before source startup. All 27 checks pass: active filtering/order/search, selection, mobile field geometry/labels/modes, disabled empty-title submission, two custom text answers, persisted records, return, reload and reopening. An unrelated campaign question is excluded. The source title deliberately uses Wrap=false/Overflow.Hidden and remains truncated. Source/data hashes, steps, console output and screenshots are retained. Ratings, attachments, manager workflows and complete usability remain unassessed. |
| Voting failure evidence | `--voting` continues the same source journey: the local UI shows one vote but the generated server retains zero after unsupported Relate. The exported Concurrent includes an unconditional Unrelate branch; this is a source race risk needing original-app review, not an authorized source rewrite. Relationship operations, two-argument datasource Patch and Concurrent still need semantic work. |
| Honest failures | Deliberately untranslatable startup, screen, button and timer behavior raises errors in the generated app; failed actions fail their journeys. |
| LLM formula boundary | Tests cover async behavior acceptance, one-expression value proposals, statement escape, explicit mutations, unknown runtime helpers and malformed response metadata. Proposals are parsed, never executed during this static gate. |
| Live LLM evidence | One OpenRouter request succeeded. Its timezone proposal passed syntax/AST checks at 0.98 confidence but reversed the offset sign; generated winter/summer journeys fail in New York, Berlin and Kolkata while UTC passes. Offline tests replay the failure and a correct candidate. Gate v3 rejects behavior helpers and known mutating methods in value expressions, while permitting pure FX sorting. Fallback receives defining-screen/local metadata and covers App/screen properties, with unverified equivalence recorded in the ledger. |

The standard test command passes 273 Python tests and 88 JavaScript tests, with
three Python skips for unavailable local exports. The consistency gate checks
bare globals and actual callable FX/FXRuntime/FX.collections exports. Fourteen generated-fixture
browser journeys pass against generated Code.gs and a persistent Sheets test
double. This is not live Google authorization or production Sheets evidence.
Local cache entries are plaintext, browser-local and capped at 1 MB encoded.
Unidentified sessions share the app's cache in the same browser profile; known
users and separate deployed script IDs have separate namespaces. An Apps Script
page still requires network access, and browser settings can remove its cache.

The current converter source fingerprint is
`a9579c68dfb1590af691411dbc8eef686d32d42a0147f6ea4b1d46106bfec6d7`.
The five-app regression census is 1,145 formulas: 1,140 translated, 971 emitted,
13 approximated and 161 ignored/unsupported. The six Microsoft exports contain
105,972 formulas: 105,861 translated, 56,923 emitted, 1,508 approximated and
47,541 ignored/unsupported. Totals now include formerly dropped App and screen
properties. All eleven pass code validation and generated-server setup/read
checks; six pass startup. The Microsoft combined usability grade is six
failed, with no complete passes.

Reproduce with Docker only:

```bash
./pfx2gas build
./pfx2gas test
./pfx2gas soak
./pfx2gas build browser
./pfx2gas browser
./pfx2gas browser scripts/assess_microsoft_samples.py
./pfx2gas browser scripts/assess_employee_workflow.py
./pfx2gas browser scripts/assess_employee_workflow.py --voting
```

The Microsoft baseline command is expected to fail while business-app blockers
remain. The populated Employee Ideas command passes its partial workflow;
adding `--voting` is an expected failure retained separately.
Artifacts are `.artifacts/benchmark/`, `.artifacts/microsoft/benchmark/` and
`.artifacts/browser/`. Browser records include input/converter hashes, console
errors, DOM measurements and screenshots. Converted screenshots are not
original Power Apps baselines. The five historical local exports, including
HelpDesk, are absent from this workspace and are not claimed as revalidated.

## Remaining acceptance work

1. Extend Employee Ideas beyond its passing populated campaign, mobile
   submission, custom text questions and reload/reopening journey to voting, ratings,
   attachments and manager workflows. Resolve remaining
   Milestones layout through source geometry dependencies. Typed
   toggle Value, canvas dimensions and dropped OnHidden initialization are fixed.
   Navigation records and screen-local variables now have generated edit/save
   evidence; populated real campaign selection now passes. Arbitrary
   context-record expressions remain unsupported. Dynamic destinations defer
   declaration until the first navigation (warned), so earlier hidden-screen
   reads can differ; transitions are not animated.
   Form/card grids now have bounded browser evidence; nested gallery cards,
   AutoHeight ordering and orientation locking remain unverified. Milestones'
   dtcSettings.Height still evaluates to a nonfinite value during startup and
   is now surfaced by the card layout gate.
   Finish required Teams/Planner/Office365 target adapters and extend the first
   actions into complete business workflows. Broader variadic and async
   semantics still need review, including Concurrent and error-handling variants.
2. Extend the implemented Dataverse contract to relationship traversal, source
   defaults/calculated fields, permissions and localized choice-to-text coercion.
   `data-contract.json` retains the exported attributes, primary keys, aliases,
   choices, relationships and views. Finish relative-date FetchXML, target
   identity migration, localized ordering and relationships. Lookup records currently persist as
   snapshots; they do not yet track changes in a related table. Dates return as
   ISO text; complete typed-date comparison behavior still needs verification.
   Do not replace external operations with empty-success stubs or invent source
   behavior to raise the score.
3. Complete and gate real business journeys per app: list/select/create/edit,
   validation/failure/delete and reload against the intended target services.
4. Obtain original-app reference states and complete exports, then verify
   critical UI interactions/layout and a real Google deployment. Only count
   an app toward the ten-app goal after its required workflows and UI checks pass.

Implementation semantics were checked against Microsoft's
[Select reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-select),
[Timer reference](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-timer),
and [Power Fx grammar](https://learn.microsoft.com/en-us/power-platform/power-fx/expression-grammar).
The record work follows Microsoft's
[record scopes and disambiguation examples](https://learn.microsoft.com/en-us/power-platform/power-fx/tables),
[operators](https://learn.microsoft.com/en-us/power-platform/power-fx/operators), and
[GroupBy/Ungroup reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-groupby), and
[If/Switch reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-if).
Storage semantics follow the
[SaveData/LoadData/ClearData reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-savedata-loaddata).
Data contract work follows Microsoft's [metadata query reference](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/webapi/query-metadata-web-api)
and [Power Fx data types](https://learn.microsoft.com/en-us/power-platform/power-fx/data-types).
The date transport follows Google's [google.script.run parameter restrictions](https://developers.google.com/apps-script/guides/html/reference/run).
Time/validation behavior follows Microsoft's [TimeValue](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-datevalue-timevalue),
[Text](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-text),
[matching](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-ismatch), and
[Find](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-find) references.
Full locale/format coverage and the newer Unicode regex dialect remain unverified.
Canvas behavior follows the source exports and Microsoft's
[responsive layout guide](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/create-responsive-layout),
[screen properties](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-screen), and
[toggle reference](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-toggle).
The LLM structural gate uses the pinned
[Acorn 8.15.0 parser](https://github.com/acornjs/acorn/tree/8.15.0).
Navigation and variable semantics follow Microsoft's
[Navigate/Back reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-navigate),
[UpdateContext reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-updatecontext), and
[variables guide](https://learn.microsoft.com/en-us/power-platform/power-fx/variables).
The timezone oracle follows the [TimeZoneOffset examples](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-dateadd-datediff).
Input modes follow the [Text input properties](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-text-input).
Voting analysis uses the [Relate/Unrelate](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-relate-unrelate)
and [Concurrent](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-concurrent) references.
Saved views follow Microsoft's [Filter reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-filter-lookup),
[FetchXML filtering](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/fetchxml/filter-rows),
[operator reference](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/fetchxml/reference/operators), and
[ordering rules](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/fetchxml/order-rows).
Card rows/minimum widths/WidthFit follow Microsoft's
[form layout rules](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/working-with-form-layout),
applied to the native FluidGrid structure found in the source export.
Conditional updates follow the [UpdateIf reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-update-updateif),
error recovery follows [IsError](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-iferror),
and text clipping/scrolling follows the [Label properties](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-text-box).
