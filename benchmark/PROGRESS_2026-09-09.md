# Conversion fidelity progress — September 9–10, 2026

The ten-app functional/usability goal is still open. Eleven pinned public
exports can be restored: the five `samples/real` apps and six Microsoft business
exports. All generate valid code. Four regression apps and Microsoft
Employee Ideas and Inspection pass the short startup check. Editable Grid now
fails on choices from the unexported Student Tracker source; the simulator
previously hid this missing dependency with an empty success. The other four Microsoft apps
fail startup on identity mappings and connector dependencies. Milestones'
non-finite settings-card layout is repaired; its first-action probe retains
the two missing user/directory migration errors and failed onboarding checks.
With explicitly migrated Users records, its original onboarding now passes
19 checks across two simulated Google users. The ownership adapter prevents
duplicate settings and repeated welcome dialogs on reload. The deeper project
probe saves its project and member through native Google People fixtures and
now passes 39 checks: three distinct names and dates, independent nested color
defaults, opening the second picker, choosing pink, the matching visible preview,
and persisted colors. Dates retain the daylight-saving change. This proves the
tested project-creation path with API fixtures. The `--workitem` probe now passes
77 checks through creation, Google-user assignment and milestone linkage, reload,
reopening with saved dropdown defaults, edits retaining the same IDs, and confirmed
deletion preserving the project/milestones. It also verifies complete reference
tables and the milestone label's source-calculated width. The collapsed headings
and chip are repaired. The deeper settings probe passes 44 checks for blank-name
validation, readable status captions and five persisted settings, then fails on
reload because the new records lack active-state defaults. Source column clipping,
broader task variants and complete usability remain open.
Inspection reaches Items Screen through its source button. With an explicitly
imported Google Sheets task board it passes all five first-action checks without
runtime errors. Missing Planner migration remains an error, so all three default
first-action probes fail. Populated Employee Ideas passes 29 checks through mobile validation,
custom text questions, submission, persistence, reload, reopening and campaign
idea counts. Its optional voting probe persists a count of one and executes
relationship operations, but fails membership: the exported Concurrent runs an
unconditional Unrelate that removes the voter link in this run.
Ratings, attachments, manager workflows and
complete app usability remain unassessed.
The native Chat notification variant adds explicit space mappings and an active
settings record; its 31 checks pass through the unchanged source notification,
saved data and reload without reposting. Google API responses remain fixtures.
No fixture or clean loading screen
is counted as a usable real app.

## Reusable fixes and evidence

| Change | Executed evidence |
|---|---|
| Editable gallery identity and scope | Two contacts boot with distinct defaults. Editing row two changes only its persisted record; typing, caret selection and the actual input node survive updates and sorting. Save/reload, reset, selector records, disabled inputs and decoded row images pass in Chromium. |
| Parent selection | A child awaits its save, queues `Select(Parent)`, finishes its remaining state updates, and invokes the source gallery handler once with the correct record. |
| Timer lifecycle | A source-shaped loading timer initializes a collection and navigates to a ready screen. Focus, elapsed Value, repeat termination, screen pause/resume, reset and restart pass with a controlled Chromium clock. |
| Common Power Fx syntax | Block/line comments, logical words and nested action chains have executed value/branch-order checks and a generated loading-flow fixture. |
| Sorting | Normalized fields, bare/qualified directions and multiple column/order pairs have executed tests; gallery reordering retains edits and focus. |
| Record aliases and disambiguation | Nested `As`, table-qualified record fields and global bypass have executed semantic tests. Gallery aliases persist through nested LookUp and awaited correct-row saves in Chromium. Global source references retain collection/Sheet routing and schema inference. |
| Projection, membership and grouping | Tests cover case-sensitive/insensitive membership, single-column tables, typed multi-column grouping and nested ForAll/Ungroup. A generated browser journey removes only the matching row and immediately updates grouped totals. |
| Conditional branches | Multi-condition If keeps every branch and optional fallback; Switch evaluates its subject once. Executed tests cover selected async actions and skipped conditions; a generated grouped-total calculation uses the later If branch. The original Microsoft exports contain over 1,200 multi-condition If calls. |
| Local inspection drafts | SaveData/LoadData/ClearData retain nested values and dates across browser reloads. Chromium verifies append behavior, app/user isolation, scoped clearing, corrupt-cache recovery and preserving the prior saved draft after a quota failure. Inspection's source cache initialization now passes the startup check. |
| Dynamic input defaults and disabled states | The browser found that a saved record loaded while its input stayed empty. Standalone input defaults are now wired; restored note/date/checkbox values, Reset and edit/caret retention through unrelated updates pass. Source DisplayMode disables Save for an empty note and enables it while typing. Existing form record handling remains intact. |
| Dataverse contracts and source keys | The decoder reads all six real exports. A native-format browser fixture edits the second row through logical field names, retains its source primary key, selects exported choice labels, creates/deletes and reloads records. Zero, false, lookup snapshots and multiple choices persist. Invalid choices, conflicting aliases and changed/missing primary keys fail without partial writes or duplicate records. |
| Real generated server gate | The real-app soak now executes generated setup, table reads and choice metadata reads. A deliberately missing setup helper fails this gate despite valid syntax. Separate execution across the six Microsoft exports covers 124 tables and 302 choice fields. |
| Fluent dates and sizing dependencies | Generated Node and Chromium journeys cover local-midnight Value/SelectedDate, independent row dates, reset, Blank, focus/disabled states, dynamic labels, bulk save and reload across DST. Date/day arithmetic preserves civil days and DateAdd leaves its input unchanged. Self.Size/Padding forward dependencies and legacy screen-size cases now execute; per-snapshot caching avoids repeated glyph-width work. Milestones saves all three edited dates with zero runtime errors; nested colors are covered by the later gallery slice. Typed Time/ISO arithmetic and exact calendar/source timezone parity remain open; Split Result alias-copy limits are ledgered. |
| Apps Script date transport | Nested Date values become ISO text before calling google.script.run. Unit tests retain the client Date and reject invalid/circular values. The browser bridge rejects raw Date objects; the native fixture saves a source Date formula through the supported transport. |
| Complete source time/validation formulas | TimeValue and Text retain fractional seconds, hour/minute semantics and output-language selection. IsMatch/MatchAll, Find and deferred IsBlankOrError reproduce the complete Inspection Manager validation formula. The unchanged Milestones timestamp formula is executed for current, older, blank and localized values. The generated UI reacts while typing and switching record/language inputs. |
| Canvas and typed controls | Source layout metadata and App/screen formulas survive parsing and enter the ledger. Chromium verifies manual/automatic containers, local DataCard child positions, numeric breakpoints, hidden-screen properties, minimum-width scrolling, uniform scaling and preserved edits/focus/caret on resize. Boolean toggle Value corrects real desktop routing and themes; slider Value and button Text retain their types. |
| Screen exit initialization | OnHidden runs on exit with the source screen's Self reference, before destination OnVisible. Awaited callbacks cannot invoke superseded entry handlers. Generated tests verify state transfer and surfaced exit errors. The original Employee Ideas loading-screen OnHidden initializes its mobile width and padding; the formerly collapsed landing page becomes readable and Browse campaigns works. |
| Navigation records and screen locals | Navigate retains literal context records, with original variable names, before target bindings/OnVisible. UpdateContext retains its defining screen across navigation and awaited saves. Generated Node and Chromium journeys select the second contact, edit only that persisted row, preserve hidden-screen bindings and local state, clear to Blank without exposing a global, and reload saved data. Runtime tests cover case-insensitive symbols, zero/false, global bypass, lazy record/local/global precedence and empty Back history. Milestones' previously dropped first-run dialog is restored. |
| Real first-action evidence | All three default solution-aware probes fail on missing migrated users or missing service migration. An additional Inspection probe with an authored imported task board passes all five checks and has zero runtime errors. Both scenarios remain in the scorecard; the passing data variant cannot overwrite the missing-migration failure or prove a complete workflow. Source, converter and solution hashes must match before evidence can be merged. |
| Microsoft services to Google | Eight Planner operations compile to the generated Sheets task-board adapter. Server and Chromium tests cover imported membership, buckets, IDs, assignments, due time, task creation, description updates, reload, failed writes and denied assignees. Input labels and control geometry pass. Missing migration/identity, unsupported operations, invalid dates and oversized records fail explicitly. Other Microsoft-service adapters and live Google execution remain open; see the Google service mapping. |
| Native Google directory | Office365Users and Microsoft365Users SearchUser, UserProfileV2 and UserPhotoV2 use Google People. The manifest enables the native service and scopes; private migration retains Microsoft IDs while mapping Google emails/resources and former aliases. Generated-server tests cover paging, selection, photos, primary fields, invalid mappings and owner/permission failures. Chromium searches, selects a profile/photo, excludes the assigned person, creates an assigned task, reloads and recovers from failures. Google API/image responses are explicit fixtures; live directory authorization remains unverified. |
| Native Google Chat | GetAllTeams, GetTeam, GetChannelsForGroup and PostMessageToChannelV3 use explicit source-ID to named-space mappings, native joined-space membership and user-authenticated messages. Generated-server tests cover mapping integrity, pagination, permission failures, HTML-to-CommonMark conversion, literal-text escaping and UTF-8 limits. Chromium selects teams/channels, posts, reloads and recovers from failures. The unchanged Employee Ideas notification variant passes 31 checks with API fixtures; no live messages or native rendering verification. |
| Dropdown display field | The Chat browser journey exposed Dropdown.Value being ignored and IDs appearing as labels. Standalone and gallery-row generation now pass that field to the runtime while retaining full selected records. A pinpoint runtime test and both browser control paths pass. |
| Computed gallery row reuse | Visual review found a literal Table recreating rows and resetting a selected channel on unrelated updates. Equal fresh unkeyed records now reuse old controls by typed, canonical values, reserving original object identities first. Runtime checks cover duplicate records, reorder and date/text distinction; Chromium retains both selected channel and exact DOM node through edits/posting. Identical clones follow prior occurrence order and unsupported/cyclic values are not matched. |
| Horizontal gallery feedback | The actual Employee Ideas/Milestones loading formula multiplies Self.TemplateWidth. The former runtime incorrectly returned the whole gallery width, producing a 33-million-pixel page. Horizontal templates expose their own width before mounting; rows use source wrapping and padding. A final row-property pass uses the gallery dimensions set by later style bindings without rerunning Items or remounting inputs. Unit/startup and Chromium checks cover repeated updates, the 224 × 88 source formula, wrapped positions, selection and full-page capture. Dynamic orientation and exact native padding/cross-axis fidelity remain unverified. |
| Responsive template sizes | Top-level TemplateSize formulas resolve lazily before dependent layout reads. Chromium retains edits and row actions through 72/84-pixel vertical and 48/64-pixel horizontal template changes, including empty/populated cards. The unchanged Milestones settings card measures 316 pixels, with zero non-finite layout errors. Cycle/non-finite guards and the helper contract have runtime regressions; nested gallery layouts remain open. |
| Connector read lifecycle | Read-only value formulas use cached asynchronous results, return Blank while pending and propagate errors through source IfError. Writes invalidate reads, including after uncertain failures; obsolete in-flight snapshots are discarded. No mutation is automatically retried. Signature, arity, effect and LLM-boundary guards have runtime regressions. |
| Startup service evidence | Unknown RPC endpoints now fail explicitly. Choices uses generated server metadata instead of the former unconditional empty success. This exposes Editable Grid's missing Student Tracker source and reduces the honest startup count from seven to six real exports. |
| Saved views | Companion solution FetchXML supplies filters missing from canvas exports. Native generated Node/Chromium tests cover nested AND/OR, typed comparisons, nulls, date instants, explicit multi-column ordering, Search, save/reload and rejected unsupported queries. Missing metadata fails on empty tables and cannot use LLM fallback. Localized choice ordering is rejected unless raw numeric order is explicitly requested; text collation and implicit primary-key ordering remain ledgered approximations. |
| Relative saved views | Last-seven-days, positive last-x-days, today, yesterday and tomorrow require explicitly exported UserLocal behavior. The target uses one browser clock/zone per query and civil dates across DST. Executed boundary and empty-table failure gates cover all supported operators. A generated Chromium journey filters, selects, saves, reloads and advances to the next local midnight. DateOnly/TimeZoneIndependent and missing behavior remain rejected; source-tenant clock, timezone settings and recent-range equivalence remain ledgered limitations. |
| Explicit date units | The generated recent-view save exposed TimeUnit.Minutes being read as an unset variable, causing subtraction of a day. All eight explicit TimeUnit members now emit as constants and execute through DateAdd in regression tests. The browser save persists exactly one minute before its fixed clock. Invalid enum members fail conversion. This does not establish complete DateAdd/DateDiff parity. |
| Startup identity | Source OnStart now waits for the actual session identity instead of capturing a permanently blank User().Email. Current-user view filters match exactly one migrated systemuser by email; absent, anonymous or duplicate matches fail. This does not implement Dataverse security. |
| Legacy template selection | A structural GalleryTemplate's OnSelect is retained when the gallery does not carry its own action. The native saved-view fixture selects the second row through Select(Parent). In the real Employee Ideas export, campaign selection now opens the correct campaign detail and its new-idea screen. |
| Native card layouts | Form/FluidGrid cards use source X/Y order and rows, minimum-width wrapping, WidthFit expansion and maximum row heights. Hidden cards leave the flow. A generated Chromium fixture verifies resize, scrolling, footer access and retained input nodes/text/focus/caret. Gallery template dimensions exist before first-row bindings. Source text overflow clips or scrolls as exported. |
| Typed collection aliases | Explicit table lineage retains exported logical/display aliases through Collect, Patch, Remove, local cache restoration and UpdateIf. Conflicts fail before mutation. Executed tests cover empty/transitive lineage, unrelated collections, first-match changes and inner-row versus outer-gallery scope. Chromium edits only the second draft, saves it through generated Code.gs, reloads and restores aliases from the local cache. |
| Source error recovery | IsError defers synchronous/async failures and distinguishes successful Blank/zero/false from errors. Direct nonfinite numeric results are errors; full Power Fx error-value propagation remains unimplemented. The unchanged real app catches the unsupported Teams post, shows its own warning and completes navigation to success. |
| Lookup snapshot aliases | Exported target-table metadata normalizes logical/display names recursively. Tests retain typed values, key-only identities and extra formula columns; ambiguous polymorphic targets, conflicting aliases and invalid nested values fail before any row write. A Chromium draft fixture reads target names through source display aliases. These are snapshots, not live relationship traversal. |
| Input modes and gallery ownership | Literal/conditional TextMode selects single-line, multiline or password controls. Chromium verifies standalone and per-row edits, transferred OnChange handlers, focus/caret and newline persistence. A pinpoint runtime regression and browser append case prevent a nested gallery template from replacing its owning gallery's template. Full nested-gallery rendering remains unverified. |
| Populated real workflow | One authored user, four campaigns and three questions are written through generated Code.gs before source startup. All 29 checks pass: active filtering/order/search, selection, mobile field geometry/labels/modes, disabled empty-title submission, two custom text answers, persisted records, return, reload, reopening and per-campaign idea counts. An unrelated campaign question is excluded. The source title deliberately uses Wrap=false/Overflow.Hidden and remains truncated. Source/data hashes, steps, console output and screenshots are retained. Ratings, attachments, manager workflows and complete usability remain unassessed. |
| Voting failure evidence | `--voting` persists a count of one and executes relationship operations, but the membership assertion fails. Every exported voting handler runs Relate and an unconditional Unrelate in Concurrent. The observed result has no voting-user link. A per-formula warning records opposing writes in separate branches; tests retain the original actions. Original-app ordering, membership after reload/removal and complete voting semantics remain unproven. |
| Concurrent scheduling | Deferred branches start independently and all finish before the following action. Success returns true; the first error in argument order propagates after siblings finish. Controlled-response generated-formula tests prove overlap and completion order. Chromium verifies two saved fields, a sibling save despite an earlier branch failure, source recovery and reload. Source error-management settings, dependency validation and external side-effect ordering remain ledgered review items. LLM gate v6 rejects behavior-only Concurrent, keyed Patch and relationship-write references in value proposals. |
| Many-to-many membership | Exported schema, navigation aliases and source keys drive symmetric links in a separate Sheets table. Generated-server and Chromium tests cover multiple members/parents, correct-record selection, retries, failure recovery, both mutation directions and reload. Refresh reads updated related-record names without refreshing the reverse source. Missing/ambiguous identities, conflicting schemas and orphaning deletes fail. No existing entity is created or deleted by a link operation. |
| One-to-many lookup relationships | Exported lookup fields drive child projections, reassignment and unlinking. Source read-only and system-required flags are enforced for relationship writes. Chromium verifies moving only the selected child, preserving an unrelated parent link, failed unlink recovery and reload. The real campaign count now changes from zero to one after submission/reload; the new guard first exposed its previously silent empty result. |
| Relationship limits | Retries are idempotent and unmatched Unrelate is a no-op in this target adapter. Explicit unlinking is required before deleting a referenced parent or a many-to-many member; source cascades and permissions are not reproduced. Alternate-key relationships, existing membership migration, source-service ordering and live Google contention remain open. Ordinary lookup fields remain stored snapshots. |
| Keyed Patch | The exported primary key and its aliases identify updates; only an absent stored key creates a new row. Tests retain other fields and typed zero/false values, reject duplicate keys and malformed inputs, and never create after validation or write failures. Native Node/Chromium journeys exercise correct-row saves, failure/retry, fixed-key create/retry and reload. Inferred/composite keys, keyless records and table arguments remain unsupported; unkeyed collections reject this overload. |
| API write locking | All generated API mutations hold a script lock across reads/writes, flush Sheets and release after errors. The server double rejects mutation reads/writes without a lock and releases without a flush attempt. Timeout, write-failure, flush-failure and retry tests pass. A failed flush can follow a write; that uncertainty is retained, and an explicit keyed retry avoids duplicates. Live Google contention and whole-workflow/client-calculation atomicity are unproven. |
| Honest failures | Deliberately untranslatable startup, screen, button and timer behavior raises errors in the generated app; failed actions fail their journeys. The browser gate now drains dispatched RPCs and checks errors after capture. A voting run exposed the previous pass-with-console-errors bug; deliberate pending-callback and late-capture probes now fail correctly. Intermediate Microsoft screenshot failures are retained without skipping later source actions. |
| LLM formula boundary | Tests cover async behavior acceptance, one-expression value proposals, statement escape, explicit mutations, unknown runtime helpers and malformed response metadata. Proposals are parsed, never executed during this static gate. |
| Live LLM evidence | One OpenRouter request succeeded. Its timezone proposal passed syntax/AST checks at 0.98 confidence but reversed the offset sign; generated winter/summer journeys fail in New York, Berlin and Kolkata while UTC passes. Offline tests replay the failure and a correct candidate. Gate v3 rejects behavior helpers and known mutating methods in value expressions, while permitting pure FX sorting. Fallback receives defining-screen/local metadata and covers App/screen properties, with unverified equivalence recorded in the ledger. |

The standard test command passes 478 Python tests and 104 JavaScript tests, with
three Python skips for unavailable local exports. The consistency gate checks
bare globals and actual callable FX/FXRuntime/FX.collections exports. Twenty-five generated-fixture
browser journeys pass against generated Code.gs and a persistent Sheets test
double. This is not live Google authorization or production Sheets evidence.
Local cache entries are plaintext, browser-local and capped at 1 MB encoded.
Unidentified sessions share the app's cache in the same browser profile; known
users and separate deployed script IDs have separate namespaces. An Apps Script
page still requires network access, and browser settings can remove its cache.

The current converter source fingerprint is
`fb7997fa94ee13a289b91cd760c3709d483c5936cc39af06b79df80231ad51e6`.
The five-app regression census is 1,145 formulas: 1,140 translated, 976 emitted,
14 approximated and 155 ignored/unsupported. The six Microsoft exports contain
105,972 formulas: 105,957 translated, 58,779 emitted, 1,872 approximated and
45,321 ignored/unsupported. Totals now include formerly dropped App and screen
properties. All eleven pass code validation and generated-server setup/read
checks; six pass startup. The Microsoft combined usability grade is six
failed, with no complete passes.

Reproduce with Docker only:

```bash
./pfx2gas build
./pfx2gas test
./pfx2gas soak
./pfx2gas build browser
./pfx2gas browser
./pfx2gas browser scripts/assess_microsoft_samples.py
./pfx2gas browser scripts/assess_milestones_workflow.py
./pfx2gas browser scripts/assess_milestones_workflow.py --project
./pfx2gas browser scripts/assess_milestones_workflow.py --workitem
./pfx2gas browser scripts/assess_employee_workflow.py
./pfx2gas browser scripts/assess_employee_workflow.py --voting
./pfx2gas browser scripts/assess_employee_workflow.py --chat
```

The five-app soak is expected to fail on Editable Grid's missing choice source.
The Microsoft baseline command is expected to fail while business-app blockers
remain. The populated Employee Ideas command passes its partial workflow;
adding `--voting` is an expected failure retained separately.
Artifacts are `.artifacts/benchmark/`, `.artifacts/microsoft/benchmark/` and
`.artifacts/browser/`. Browser records include input/converter hashes, console
errors, DOM measurements and screenshots. Converted screenshots are not
original Power Apps baselines. The five historical local exports, including
HelpDesk, are absent from this workspace and are not claimed as revalidated.

This relationship checkpoint is reproduced in `.artifacts/test-relationships-idempotent.log`,
`.artifacts/browser-relationships-release.log`, `.artifacts/employee-relationships-release.log`
and `.artifacts/voting-relationships-release.log`. The rebuilt production CLI also
passes generated-client startup and actual Code.gs lookup/join/lock checks in
`.artifacts/baked-relationships-release-proof.log`. All 35 checks before the real
voter-membership assertion pass; that assertion fails with no console errors,
because the source concurrently removes the newly added link. Original source
app and solution bytes are unchanged.

The relative-view checkpoint is reproduced in
`.artifacts/test-relative-timeunit-release.log`,
`.artifacts/browser-relative-timeunit-release.log`,
`.artifacts/soak-relative-timeunit-release.log` and
`.artifacts/microsoft-relative-timeunit-release.log`. The rebuilt CLI passes
generated startup and its baked date-boundary check in
`.artifacts/baked-relative-timeunit-proof.log`. The saved/reloaded fixture UI
was visually inspected in `.artifacts/browser/relative-saved-views/`.
Fresh Employee Ideas and voting results retain 29 passing checks and the
expected membership failure after 35 checks, respectively, in
`.artifacts/employee-relative-timeunit-release.log` and
`.artifacts/voting-relative-timeunit-release.log`. These are historical checkpoint
records; the current Google-adapter evidence is below.

The earlier Planner-adapter checkpoint is reproduced in `.artifacts/test-google-planner-release.log`
(372 Python/93 JavaScript), `.artifacts/browser-google-planner-release.log`
(17 fixtures including board layout/label checks plus three deliberate failure gates). The board screenshot was
visually reviewed. `.artifacts/soak-google-planner-release.log` retains Editable
Grid's failure; `.artifacts/microsoft-google-planner-release.log` retains all
three default first-action failures and the migrated Inspection pass.
`.artifacts/employee-google-planner-release.log` and
`.artifacts/voting-google-planner-release.log` retain 29 passing checks and the
expected voting membership failure after 35 checks. The rebuilt CLI's generated
server import/create and missing-migration guard pass in
`.artifacts/baked-google-planner-proof.log`. `.artifacts/google-planner-release-proof.log`
checks matching source fingerprints and all final verdicts. No live Google
authorization or persistence is claimed. Reviewed membership/identity migration
and the workbook access boundary remain deployment prerequisites.

The native-directory checkpoint is in `.artifacts/test-google-directory-full.log`
(392 Python/93 JavaScript) and `.artifacts/browser-google-directory-full.log`
(18 passing fixtures and three deliberate failure gates). The directory fixture's
assignment/photo screenshot was visually reviewed. The rebuilt CLI's native
manifest, import, search/profile lookup and missing-migration rejection pass in
`.artifacts/baked-google-directory-proof.log`. Real-app evidence is in
`.artifacts/soak-google-directory.log`, `.artifacts/microsoft-google-directory.log`,
`.artifacts/employee-google-directory.log` and `.artifacts/voting-google-directory.log`.
The initial real first-action run included a Chromium crash during Employee
Ideas; it is retained in `.artifacts/microsoft/first-actions-before-directory-retry.json`.
A separate run without concurrent large browser jobs refreshes first actions
and the scorecard in `.artifacts/first-actions-google-directory-retry.log`.
It reproduces the crash. A viewport-only diagnostic in
`.artifacts/employee-directory-diagnostic.log` finds a 33,554,432-pixel page:
the loading gallery's horizontal TemplateWidth incorrectly equals its whole
width, and the source Width formula repeatedly multiplies it. The same result
occurs with the new directory contract disabled; neither run makes a People
request. The subsequent static-template geometry fix is described above. The diagnostic
does not replace the failed full-page usability evidence.
No missing migration or failed real-app workflow is promoted to a pass.

The previous horizontal-gallery checkpoint passes 393 Python/94 JavaScript tests
in `.artifacts/test-horizontal-gallery-settled.log` and all 19 Chromium fixtures
in `.artifacts/browser-horizontal-gallery-release.log`, including the three
deliberate failure gates. The fixed gallery fixture was visually reviewed.
`.artifacts/microsoft-horizontal-gallery-release.log` and
`.artifacts/soak-horizontal-gallery-release.log` preserve six bootable apps out
of eleven and zero complete usability acceptances. Employee Ideas' unmigrated
first-action run now completes all captures without crashing; its missing-user
initialization still fails. Its mobile loading screenshot was visually reviewed.
The populated submission journey still passes in
`.artifacts/employee-horizontal-gallery-release.log`. The baked CLI metadata,
runtime and generated startup pass in `.artifacts/baked-horizontal-gallery-proof.log`.
The optional voting run still fails membership after 35 passing checks in
`.artifacts/voting-horizontal-gallery-release.log`; its source Unrelate behavior
is unchanged. `.artifacts/horizontal-gallery-release-proof.log` checks current
source fingerprints, every final verdict and the real loading gallery's 224 × 88
geometry. The implementation follows the orientation-specific TemplateSize
contract in Microsoft's [gallery documentation](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-gallery).

The preceding Chat checkpoint passes 432 Python/95 JavaScript tests in
`.artifacts/test-chat-release.log`, including generated native-service execution,
and all 20 Chromium fixtures in `.artifacts/browser-chat-release.log`. The three
intentional evidence failures remain failures. The successful Chat picker/post
screen is captured in `.artifacts/browser/google-chat/native-notification.png`.
`.artifacts/employee-chat-release.log` records 31 passing checks in the unchanged
Employee Ideas app. Its `native-chat-evidence.json` retains the original connector
arguments, exact native request and returned message ID. An explicit active status
was necessary for the source's Active Employee Idea Settings view to include the
authored migration record; no source formula or app state was overridden.

The default populated journey still passes 29 checks in
`.artifacts/employee-chat-default-release.log`; the voting variant still fails
membership after 35 passing checks in `.artifacts/voting-chat-release.log`.
`.artifacts/soak-chat-release.log` and `.artifacts/microsoft-chat-release.log`
retain the same six bootable real exports and zero complete usability passes.
All eleven still pass generated code/server checks. The baked CLI's current
Chat server, runtime, manifest, private migration and startup are verified in
`.artifacts/baked-chat-proof.log`. Native Google API permissions, delivery and
message rendering remain unverified; fixture success is not live deployment.
`.artifacts/chat-release-proof.log` reconciles final converter fingerprints,
all fixture/real-app verdicts, the 31/29/35-check scenarios and the original
team/channel IDs in the single retained native notification request.

The responsive-gallery checkpoint passes 433 Python/96 JavaScript tests in
`.artifacts/test-responsive-gallery.log`; the final helper signature check is in
`.artifacts/consistency-responsive-gallery.log`. All 21 Chromium fixtures pass
in `.artifacts/browser-responsive-gallery-release.log`, with the three deliberate
failure gates retained. The new fixture exercises initially empty cards,
72/84-pixel responsive vertical templates, 48/64-pixel horizontal templates,
keyboard and pointer selection, clearing and retained edited text/input nodes.
Screenshots in `.artifacts/browser/responsive-gallery/` were visually reviewed.

The actual Milestones source uses a screen-size Switch for TemplateSize and
`Self.TemplateHeight*CountRows(Self.AllItems)` for Height. The template formula
was previously omitted. It now resolves before dependent layouts, eliminating
the 33 non-finite settings-card errors seen at the Chat checkpoint. The unchanged
source's empty gallery is zero pixels high, its team-member gallery is 245,
its milestone container is 296 and its card is 316. Exact measurements are in
`.artifacts/browser/microsoft-first-action-milestones/settings-layout.json`.
The first-action probe has seven passing checks, four failed onboarding checks
and two explicit identity/directory migration errors; it remains a failure.
The implementation follows Microsoft's [gallery TemplateSize contract](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-gallery).
Dynamic orientation, padding, WrapCount and nested-gallery layouts remain open.

`.artifacts/soak-responsive-gallery.log` and `.artifacts/microsoft-responsive-gallery.log`
retain eleven valid code/server conversions, six startup passes and zero complete
usability acceptances. The populated Employee Ideas variants retain 29 default
and 31 Chat checks with zero errors, while voting retains its membership failure
after 35 checks: `.artifacts/employee-responsive-gallery.log`,
`.artifacts/employee-chat-responsive-gallery.log`, `.artifacts/voting-responsive-gallery.log`.
The rebuilt CLI's emitted runtime and generated startup pass in
`.artifacts/baked-responsive-gallery-proof.log`. Final hashes and verdicts are
reconciled in `.artifacts/responsive-gallery-release-proof.log`.

The ownership checkpoint passes 447 Python/96 JavaScript tests in
`.artifacts/test-ownership-release.log` and all 21 Chromium fixtures in
`.artifacts/browser-ownership-release.log`. The generated ledger and data
contract identify native Owner fields and their migrated principals; validation
rejects dropped ownership metadata. Source user/team assignment, ordinary-edit
retention, aliases, missing/duplicate identity, ignored derived-column writes and
failure without partial mutation have generated-server regressions.

`.artifacts/milestones-ownership-release.log` records 19 passing unchanged-source
onboarding checks across two simulated Google users. Saved settings retain their
owners and dismissal preferences across reload. The project extension in
`.artifacts/milestones-project-ownership-release.log` passes 28 checks, calls
native Google People with the exact mapped resource, saves a project/member,
and then fails its distinct milestone-name assertion. Three rows all contain
"Survey site", null dates and fallback gray colors. The full records and native
request trace were retained with that run. The next checkpoint fixes the row
names and advances this probe to the missing date support; neither checkpoint
establishes a successful project conversion.

The public and Microsoft censuses remain eleven valid code/server conversions,
six startup passes and zero complete usability acceptances, in
`.artifacts/soak-ownership-release.log` and `.artifacts/microsoft-ownership-release.log`.
Default first-action failures remain in `.artifacts/first-actions-ownership-release.log`.
Employee Ideas retains 29 default and 31 native Chat checks with no runtime errors;
voting retains its membership failure after 35 checks. Logs are
`.artifacts/employee-ownership-release.log`, `.artifacts/employee-chat-ownership-release.log`
and `.artifacts/voting-ownership-release.log`. The rebuilt CLI's actual ownership
creation/update path passes in `.artifacts/baked-ownership-proof.log`.
`.artifacts/ownership-release-proof.log` reconciles current source hashes and verdicts.

Ownership follows Microsoft's [create contract](https://learn.microsoft.com/en-us/dotnet/api/microsoft.xrm.sdk.iorganizationservice.create?view=dataverse-sdk-latest).
It uses the native Google caller and a migrated Dataverse Users email/key;
Office365Users directory mappings remain separate. Stored ownership does not
reproduce source row security, assignment privileges, business units, cascading
assignment, audit metadata or status defaults.

The AllItems checkpoint passes 448 Python/96 JavaScript tests in
`.artifacts/test-gallery-allitems.log`, the expanded generated bulk-save
regression in `.artifacts/startup-gallery-allitems.log`, and all 21 Chromium
fixtures in `.artifacts/browser-gallery-allitems.log`. Per-row controls are
available through AllItems record scopes and aliases, with a lazy fallback for
controls outside the record. Blank fields retain normal scope precedence.
The browser edits both rows, reverses their order, saves through awaited source
actions and reloads distinct persisted values. Control references omit DOM
nodes from data serialization and leave source records untouched.

The unchanged Milestones project saves "Survey site", "Replace equipment" and
"Review handover" in separate records. Its 29 checks pass before the new
`milestone-default-dates-persist` assertion fails on three null dates; fallback
gray colors also remain. The final failure, records and native People request
trace are in `.artifacts/milestones-project-gallery-allitems-release.log` and
`.artifacts/browser/milestones-populated-project/`. The source date control is
`Microsoft_CoreControls_DatePicker`; its Value formula adds weekly offsets to
Today(). This historical failure is repaired by the later date and nested-gallery checkpoints.

Two-user onboarding retains 19 checks in `.artifacts/milestones-gallery-allitems.log`.
Employee Ideas retains 29 submission checks, 31 native Chat checks and its voting
membership failure after 35 checks in `.artifacts/employee-gallery-allitems.log`,
`.artifacts/employee-chat-gallery-allitems.log` and `.artifacts/voting-gallery-allitems.log`.
The real censuses retain eleven code/server passes, six startup passes and zero
complete usability acceptances in `.artifacts/soak-gallery-allitems.log` and
`.artifacts/microsoft-gallery-allitems.log`. The rebuilt CLI's bulk-save path
passes in `.artifacts/baked-gallery-allitems-proof.log`. Source fingerprints and
verdicts are reconciled in `.artifacts/gallery-allitems-release-proof.log`.
AllItems behavior follows Microsoft's [Gallery reference](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-gallery).
Gallery loading remains eager. Nested rendering was still incomplete at that checkpoint; the following slice adds scoped instances.

## Nested color selection and project creation — September 10

Each parent row now owns a separate nested gallery controller. Source Defaults
select matching loaded records; first-row fallback, IsSelected, row references
and explicit parent selection stay scoped. Reset scrolls/selects the requested
gallery and leaves its children's state intact. The generated fixture covers
items disappearing/reappearing, nested edits and node identity through parent
sorting and template resizing, pointer/keyboard choice and bulk save/reload.
The Node simulator now handles balanced nested templates and scoped child lookup.

The real Milestones picker initially selected blue when pink was clicked: its
transparent long label intercepted a pointer outside the blue button's bounds.
Clipping generated button content fixes that defect, with the long-label case
retained in the fixture. The source app's 39 creation checks now pass, including
the second milestone's pink preview and persisted `#F4B9B9`; its first and third
milestones retain their own colors. Names and DST-aware dates remain distinct.
Native Google People responses are fixtures, and full app usability remains open.

Cross-app checks caught an alias-copy regression before this checkpoint:
Employee Ideas saved its responses but could not reopen them. Gallery row and
AllItems records now retain non-enumerable logical-name aliases and relationship
source metadata. The runtime regression checks alias reads without adding UI
fields to source records; the actual Employee Ideas reopening probe passes again.

Final logs are `.artifacts/test-nested-gallery-final.log`,
`.artifacts/browser-nested-gallery-final.log`,
`.artifacts/soak-nested-gallery-final.log`,
`.artifacts/microsoft-nested-gallery-final.log`,
`.artifacts/milestones-nested-gallery-final.log` and
`.artifacts/employee-nested-gallery-final.log`. The rebuilt CLI execution is in
`.artifacts/baked-nested-gallery-proof.log`; artifact reconciliation is in
`.artifacts/nested-gallery-release-proof.log`. Current screenshots and the actual
pointer/selection record are in `.artifacts/browser/milestones-populated-project/`.
These are partial real workflows and synthetic regression cases, not ten
complete real-app usability acceptances. Cross-level record aliases, additional
nested control types and full source visual comparisons remain unverified.

## Work-item lifecycle and selector defaults — September 10

That checkpoint passed 75 checks in the unchanged Milestones source with authored user migration
and native People API fixtures. It creates "Survey the north entrance", assigns
Ada Lovelace's migrated Google identity and the "Replace equipment" milestone,
and reloads those records. Reopening restores the title, multiline description,
date and both selected lookup records. Editing the title to "Confirm north entrance
clearance" and moving the due date from March 9 to March 11 retains the work-item,
assignee and milestone IDs. Confirmed deletion removes the work item and leaves
the project and its three milestones.

This exposed three reusable defects. Referenced Font/Size/Padding and sibling
properties were absent from gallery rows; scoped definitions now resolve them
without leaking between parent/child instances or falsely detecting cycles across
rows. SELECT defaults were applied only once and lost their record type on Reset;
standalone and row selectors now share the runtime path, retain record/table/Blank
defaults and preserve unrelated user edits. Chromium additionally requires an
explicit `selectedIndex = -1` to keep Blank from selecting its first option.
Finally, a checkbox click bubbled into the source gallery action and reopened the
editor; native editing no longer triggers gallery selection unless the source
explicitly requests it. Regression cases retain explicit `Select(Parent)` behavior.

Scoped DOM caching rejects disconnected nodes and instances moved to another row;
global lookups do not cache a gallery's first child across sorting. Control reads
defer geometry measurement when inline dimensions suffice, while fallback reads
remain fresh. The final local work-item-open observation is 2.37 seconds, below
the journey's unchanged five-second action timeout. This is a local measurement,
not a deployment performance guarantee.

All 459 Python and 103 JavaScript tests pass in `.artifacts/test-workitem-final.log`;
all 23 Chromium fixtures pass in `.artifacts/browser-workitem-final.log`. Runtime
signature and callable-surface checks pass. The lifecycle evidence is in
`.artifacts/milestones-workitem-lifecycle-final.log` and
`.artifacts/browser/milestones-populated-workitem/`, including before/after records,
Google request traces, timing and visually reviewed reload/deletion screenshots.
The production image was rebuilt; its scoped generated startup/persistence passes
in `.artifacts/baked-workitem-proof.log`. Real corpus verdicts are retained in
`.artifacts/soak-workitem-final.log` and `.artifacts/microsoft-workitem-final.log`.
Employee Ideas submission and native Chat retain 29/31 passing checks; voting still
fails membership after 35 checks in `.artifacts/employee-workitem-final.log`.
`.artifacts/workitem-release-proof.log` reconciles current source hashes, counts,
record identities and all verdicts. No real Google messages were sent.

The workflow pass did not establish full UI fidelity. Screenshots then showed clipped
headings and a narrow milestone chip. Inspection found 194 exported character-width
reference records but only 100 generated seed rows, and differing font/weight enum
representations in the lookup operands. The following checkpoint repairs both. Global
status/category/priority setup and Dataverse status defaults remain unassessed;
OnCheck/OnUncheck, broader task variants and original-app visual comparison remain
open. Reset/default behavior follows Microsoft's
[Reset reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-reset)
and [Combo box contract](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-combo-box).

## Complete reference tables and source typography — September 10

DataInit previously sliced every exported table to 100 records. That removed
29 Milestones icons, 473 localization records and 94 character-width records.
Initialization now preserves every exported seed row, including external-source
snapshots. It expands Google Sheets rows/columns before header/body writes; API
creates also expand capacity after a full import. Repeated setup keeps existing
data. This does not fetch tenant records absent from the export or backfill a
workbook initialized by an older converter.

The source character-width table stores Segoe UI as
`'Segoe UI', 'Open Sans', sans-serif`, with `normal` and `600` weights. The emitter
previously used the display names, so lookups returned Blank and collapsed label
widths to padding alone. Segoe UI and the four supported FontWeight members now
retain their underlying CSS values; rendering preserves separate fallback faces
instead of joining them into a nonexistent font. Executed source-shaped lookups
cover normal/semibold metrics. The existing nested-row fixture checks semantic
font reads and the actual CSS stack.

Milestones now passes 77 checks, retaining the 75-check lifecycle and verifying all
three reference-table counts plus the original milestone-width formula. Every
character matches its source metric, and the rendered label is 115 pixels, exactly
the source formula's cap at this viewport. The reviewed screenshot shows readable
"Milestone status" and "Tracking" headings and restored settings/search icons.
Some text remains clipped by the source's column widths; full original-app visual
comparison, font availability and broader enum mappings are still unverified.

The new generated-server regression retains 1,005 records in each of a static and
external table with 29 fields, appends another record, initializes 30 choice
columns and verifies repeated setup preserves data. The test double now rejects
out-of-grid ranges. `.artifacts/test-seeds-fonts-initial.log` passes 461 Python and
103 JavaScript tests, and `.artifacts/browser-seeds-fonts-initial.log` passes all
23 fixtures and preserves the three deliberate failure verdicts. The real journey
and label metrics are in `.artifacts/milestones-seeds-fonts-initial.log` and
`.artifacts/browser/milestones-populated-workitem/milestone-label-metrics.json`.

Both images were rebuilt. `.artifacts/baked-reference-proof.log` confirms the
production CLI generates valid code and preserves every exported seed count across
Milestones' 18 tables under the stricter Sheets double. The public and Microsoft
censuses are in `.artifacts/soak-seeds-fonts-final.log` and
`.artifacts/microsoft-seeds-fonts-final.log`: eleven code/server passes, six startup
passes and zero complete usability acceptances. Employee Ideas retains its 29/31
submission/Chat checks and known membership failure after 35 checks in
`.artifacts/employee-seeds-fonts-final.log`. Final hashes, counts and verdicts are
reconciled in `.artifacts/seeds-fonts-release-proof.log`.

The font-stack representation is consistent with Microsoft's
[canvas theme format](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/modern-controls/modern-theming)
and the pinned source table. Grid expansion uses Google's
[Sheet capacity and insertion methods](https://developers.google.com/apps-script/reference/spreadsheet/sheet).
These results use API fixtures and a Sheets test double, not live Google deployment.

## Source control blank checks and expression precedence

The deeper Milestones `--settings` probe exercises the original category,
priority and status editors before project/work-item creation. It exposed two
converter defects: `IsBlank(txtCategoryName)` tested a control record instead
of its text, and `"Optional status " & ThisItem.Order - 1` subtracted from a
concatenated string. Neither source formula was changed.

Legacy control templates now retain their primary output from the exact exported
template name/version. `IsBlank` and `IsBlankOrError` coerce only tagged control
references, including row controls copied through gallery AllItems; ordinary
records retain their own blank semantics. The exported `powerfxv1` flag disables
the legacy coercion. A symbol preserves the reference metadata through object
copies without adding source fields or JSON payload properties. Unsupported
primary outputs raise an error when read. Missing, conflicting or unmatched
template contracts are not inferred, and other implicit-control coercions remain
outside this change.

Arithmetic now binds more tightly than concatenation, which binds more tightly
than comparison; membership binds below comparison. Source-formula execution,
generated startup journeys and Chromium fixtures cover both compatibility modes,
row edits, save validation and readable arithmetic captions. These rules follow
Microsoft's [IsBlank implementation](https://github.com/microsoft/Power-Fx/blob/main/src/libraries/Microsoft.PowerFx.Core/Texl/Builtins/IsBlank.cs),
[V1 feature settings](https://github.com/microsoft/Power-Fx/blob/main/src/libraries/Microsoft.PowerFx.Core/Public/Config/Features.cs)
and [operator precedence](https://github.com/microsoft/Power-Fx/blob/main/src/libraries/Microsoft.PowerFx.Core/Syntax/Precedence.cs).

The settings probe preserves its failure on reload: all five named records save,
but their `statecode`, `statuscode` and `createdon` are Blank. The original active
views therefore omit them. Dataverse default/audit-field migration and complete
settings/work-item behavior remain open; no example rows are patched merely to
make this test pass.

Final verification: 478 Python tests (three skips), 104 JavaScript tests and the
24-bare-call consistency gate pass in `.artifacts/test-primary-precedence-final.log`.
All 25 Chromium fixtures pass in `.artifacts/browser-primary-precedence-final.log`,
including both control compatibility modes; all three intentional failure gates
remain failed. The Milestones work-item journey retains 77 passing checks; settings
has 44 passing checks, zero runtime errors and one failed reload assertion in
`.artifacts/milestones-primary-precedence-final.log`. Its `settings-draft.png` was
visually reviewed with readable completion/optional-status labels and five named
inputs. These are automated usability probes, not human or original-platform
comparisons.

The production image was rebuilt and its actual CLI output retains the three
settings inputs' exported text-output contracts. The eleven-app census and
Employee Ideas' 29/31 passing checks and known voting-membership failure after
35 checks are unchanged. Final source fingerprints, test counts, saved rows and
failure reasons reconcile in `.artifacts/primary-precedence-release-proof.log`.
This remains fixture-backed evidence with six real-app startup passes and zero
complete usability acceptances.

## Remaining acceptance work

1. Extend Employee Ideas beyond its passing populated campaign, mobile
   submission, custom text questions and reload/reopening journey to voting, ratings,
   attachments and manager workflows. Extend Milestones beyond its passing
   project creation and work-item create/edit/delete path to settings/status
   initialization, additional task variants, readable labels and complete workflows.
   Onboarding now has separate-user persistence evidence. Typed
   toggle Value, canvas dimensions and dropped OnHidden initialization are fixed.
   Navigation records and screen-local variables now have generated edit/save
   evidence; populated real campaign selection now passes. Arbitrary
   context-record expressions remain unsupported. Dynamic destinations defer
   declaration until the first navigation (warned), so earlier hidden-screen
   reads can differ; transitions are not animated.
   Form/card grids now have bounded browser evidence; nested gallery cards,
   AutoHeight ordering and orientation locking remain unverified. Milestones'
   settings-card dimensions now pass; other screen geometry still needs review.
   Extend the implemented Google Chat, People and Planner subsets through
   complete real inspection workflows. Extend the first
   actions into complete business workflows. Broader variadic and async
   semantics still need review, including source Concurrent settings/dependencies,
   Patch overloads and error-handling variants.
2. Extend the implemented Dataverse contract to remaining relationship semantics, source
   defaults/calculated fields, permissions and localized choice-to-text coercion.
   `data-contract.json` retains the exported attributes, primary keys, aliases,
   choices, relationships and views. Extend relative-date FetchXML, preserve
   legacy bare date units and whole-unit DateDiff boundaries, and implement target
   identity migration, localized ordering, alternate keys and source cascades. Lookup fields currently persist as
   snapshots; they do not yet track changes in a related table. Dates return as
   ISO text; complete typed-date comparison behavior still needs verification.
   Do not replace external operations with empty-success stubs or invent source
   behavior to raise the score.
3. Complete and gate real business journeys per app: list/select/create/edit,
   validation/failure/delete and reload against the intended target services.
4. Obtain original-app reference states and complete exports, then verify
   critical UI interactions/layout and a real Google deployment. Only count
   an app toward the ten-app goal after its required workflows and UI checks pass.

Implementation semantics were checked against Microsoft's
[Select reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-select),
[Timer reference](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-timer),
and [Power Fx grammar](https://learn.microsoft.com/en-us/power-platform/power-fx/expression-grammar).
The record work follows Microsoft's
[record scopes and disambiguation examples](https://learn.microsoft.com/en-us/power-platform/power-fx/tables),
[operators](https://learn.microsoft.com/en-us/power-platform/power-fx/operators), and
[GroupBy/Ungroup reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-groupby), and
[If/Switch reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-if).
Storage semantics follow the
[SaveData/LoadData/ClearData reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-savedata-loaddata).
Data contract work follows Microsoft's [metadata query reference](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/webapi/query-metadata-web-api)
and [Power Fx data types](https://learn.microsoft.com/en-us/power-platform/power-fx/data-types).
The date transport follows Google's [google.script.run parameter restrictions](https://developers.google.com/apps-script/guides/html/reference/run).
Time/validation behavior follows Microsoft's [TimeValue](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-datevalue-timevalue),
[Text](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-text),
[matching](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-ismatch), and
[Find](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-find) references.
Full locale/format coverage and the newer Unicode regex dialect remain unverified.
Canvas behavior follows the source exports and Microsoft's
[responsive layout guide](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/create-responsive-layout),
[screen properties](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-screen), and
[toggle reference](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-toggle).
The LLM structural gate uses the pinned
[Acorn 8.15.0 parser](https://github.com/acornjs/acorn/tree/8.15.0).
Navigation and variable semantics follow Microsoft's
[Navigate/Back reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-navigate),
[UpdateContext reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-updatecontext), and
[variables guide](https://learn.microsoft.com/en-us/power-platform/power-fx/variables).
The timezone oracle follows the [TimeZoneOffset examples](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-dateadd-datediff).
Input modes follow the [Text input properties](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-text-input).
Voting analysis uses the [Relate/Unrelate](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-relate-unrelate)
and [Concurrent](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-concurrent) references.
The two-argument datasource Patch overload is confirmed in Microsoft's
[Power Fx implementation](https://github.com/microsoft/Power-Fx/blob/main/src/libraries/Microsoft.PowerFx.Core/Texl/Builtins/Patch.cs)
(`PatchSingleRecordFunction`). Its
[interpreter](https://github.com/microsoft/Power-Fx/blob/main/src/libraries/Microsoft.PowerFx.Interpreter/Functions/LibraryMutation.cs)
attempts an update and appends only after a NotFound result. This is now exercised
with explicit exported primary keys. Microsoft's
[TableValue contract](https://github.com/microsoft/Power-Fx/blob/main/src/libraries/Microsoft.PowerFx.Core/Public/Values/TableValue.cs)
requires primary-key semantics for this overload; inferred/composite keys and
keyless/table arguments remain unsupported here. Generated write serialization
follows Google's [script locks](https://developers.google.com/apps-script/reference/lock/lock-service)
and [flush-before-release guidance](https://developers.google.com/apps-script/reference/lock/lock).
Saved views follow Microsoft's [Filter reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-filter-lookup),
[FetchXML filtering](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/fetchxml/filter-rows),
[operator reference](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/fetchxml/reference/operators), and
[ordering rules](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/fetchxml/order-rows).
Relative date behavior follows Microsoft's
[date-column metadata contract](https://learn.microsoft.com/en-us/power-apps/developer/data-platform/behavior-format-date-time-attribute).
The recent-day cutoff interpretation is consistent with the
[SQL 4 CDS converter implementation](https://github.com/MarkMpn/Sql4Cds/blob/master/MarkMpn.Sql4Cds.Engine/FetchXml2Sql.cs)
(midnight N days ago, exclusive current instant); this independent implementation
is corroborating evidence, not a live Dataverse oracle. Boundary fidelity remains
explicitly unverified. Explicit date units follow Microsoft's
[DateAdd reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-dateadd-datediff).
Card rows/minimum widths/WidthFit follow Microsoft's
[form layout rules](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/working-with-form-layout),
applied to the native FluidGrid structure found in the source export.
Conditional updates follow the [UpdateIf reference](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-update-updateif),
error recovery follows [IsError](https://learn.microsoft.com/en-us/power-platform/power-fx/reference/function-iferror),
and text clipping/scrolling follows the [Label properties](https://learn.microsoft.com/en-us/power-apps/maker/canvas-apps/controls/control-text-box).
